// Use relative API base so the Vite dev proxy can forward requests to the backend.
export const API_BASE_URL = '/api';

// Default export for compatibility with different import styles
export default {
    API_BASE_URL
};